import React from 'react';
import s from './Data.module.css';

const Data = (props) => {
    return (
        <div className={s.div}>
            last seen yestersay
        </div>
    );
}

export default Data;